extends layout

block content
  h1= title
  p Express site with a very basic inventory system

  h1 Dynamic Content

  if error
    p Error getting dynamic content.
  else
    p The inventory has the following records.

    ul
      li Items: !{data.item_count}
      li Categories: !{data.category_count}
  
